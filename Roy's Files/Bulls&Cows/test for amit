private static int countCows(int[] copyGuess, int[] guess) {
        int cows = 0;
        for (int i = 0; i < guess.length; i++) {
            for (int j = 0; j < copyGuess.length; j++) {
                if (i != j && guess[i] == copyGuess[j]) {
                    cows++;
                    break; // Break to avoid double counting
                }
            }
        }
        return cows;
    }
